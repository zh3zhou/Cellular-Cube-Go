# Agent rules

The repository-level contract is [`../../AGENTS.md`](../../AGENTS.md).
Product intent and current scope are in
[`../../PROJECT_CONTEXT.md`](../../PROJECT_CONTEXT.md).

Those files replace the historical absolute paths, private-log requirements,
duplicated game-loop snippets, and obsolete Web-serving instructions that were
previously stored here. Follow the root contract and verify commands against
the current repository before use.
